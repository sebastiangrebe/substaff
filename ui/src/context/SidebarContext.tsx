// Re-export shadcn sidebar context so existing imports keep working.
// The canonical provider & hook now live in @/components/ui/sidebar.
export { SidebarProvider, useSidebar } from "@/components/ui/sidebar";
